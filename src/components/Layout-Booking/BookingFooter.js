import classes from './BookingFooter.css';

const Footer = () => {
    return (
        <footer className={classes.footer}>
        <nav>
            <ul>
                <li>
                    
                </li>
            </ul>
        </nav>
    </footer>
    );
};

export default Footer;